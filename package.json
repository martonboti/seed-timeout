{
  "name": "seed-timeout-api",
  "version": "1.0.0",
  "main": "app.js",
  "author": "Boti",
  "license": "MIT",
  "scripts": {
    "start": "node app.js",
    "test": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha tests/**/*.test.js",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "10.1.0"
  },
  "dependencies": {
    "express": "^4.16.3",
    "mongodb": "^3.1.1",
    "mongoose": "^5.2.1",
    "validator": "^10.4.0"
  },
  "devDependencies": {
    "faker": "^4.1.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5"
  }
}
